export enum tagTypes {
  auth = "auth",
  user = "user",
  admin = "admin",
  transaction = "transaction",
}

export const tagTypesList = [
  tagTypes.auth,
  tagTypes.user,
  tagTypes.admin,
  tagTypes.transaction,
];